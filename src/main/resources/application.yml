spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/coinx?user=root
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    generate-ddl: true
    hibernate:
      ddl-auto: update
